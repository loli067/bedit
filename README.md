# Bedit
A custom script for Boot image editor

#How to Install
```
sudo apt install git device-tree-compiler lz4 xz-utils zlib1g-dev openjdk-17-jdk gcc g++ python3 python-is-python3 p7zip-full android-sdk-libsparse-utils && git clone https://github.com/Logic067/bedit && sh setup.sh
```
